default_model: base
models:
  base:
    name: base-smoke
    path: models/base.safetensors
    format: safetensors
schedulers:
  euler:
    steps: 10
    beta_start: 0.0001
    beta_end: 0.02
default_scheduler: euler
required_nodes:
  - txt2img
  - img2img
