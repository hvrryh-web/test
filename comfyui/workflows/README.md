# Workflows

Store exported ComfyUI workflow JSON graphs in this directory. Keep file names descriptive (e.g., `sdxl_base_inpaint.json`) and document required checkpoints or custom nodes in comments within the JSON or alongside a `.md` note.
